# Install nodeJs
      - name: Use Node.js
        uses: actions/setup-node@v2
        with: 
          node-version: "14.x"

      # Install project dependencies
      - name: Install dependencies
        run: npm install
